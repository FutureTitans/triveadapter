fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
anthropic>=0.100.0
plotly==5.22.0
nilearn==0.10.4
nibabel>=4.0.0
numpy<2.3
python-dotenv==1.0.1
aiofiles==24.1.0
# torch, torchvision, torchaudio are installed separately in the Dockerfile
# (CPU-only builds from https://download.pytorch.org/whl/cpu).
# Do NOT add them here — version constraints here can cause pip to downgrade
# torch while leaving torchvision at a mismatched version, breaking C++ ops.
